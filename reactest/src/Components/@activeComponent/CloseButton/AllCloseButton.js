import CloseButton from 'react-bootstrap/CloseButton';

function AllCloseButton() {
    return (

        <CloseButton aria-label="Hide" variant="white" disabled />;

    <div className="bg-dark p-3">
        <CloseButton aria-label="Hide" variant="white" disabled />
        <CloseButton aria-label="Hide" variant="white" disabled />
    </div>
    );
}

export default AllCloseButton;
